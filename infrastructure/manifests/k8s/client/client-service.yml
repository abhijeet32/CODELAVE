apiVersion: v1
kind: Service
metadata:
  name: codelave-client-svc
  labels:
    app: codelave-client

spec:
  type: ClusterIP
  selector:
    app: codelave-client
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP